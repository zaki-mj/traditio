name: traditional_gems
description: "Traditional Gems — discover Algeria's traditional places"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  flutter_localizations:
    sdk: flutter
  url_launcher: ^6.1.11
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  shared_preferences: ^2.0.18
  firebase_auth: ^6.1.2
  firebase_storage: ^13.0.5
  image_picker: ^1.2.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
  - assets/pictures/
  - assets/data/
